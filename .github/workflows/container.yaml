name: deploy-container

on:
  push:
    branches:
      - main
    paths:
      - "packages/container/**"

defaults:
  run:
    working-directory: "packages/container"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build

      - uses: shinyinc/action-aws-cli@v1.2
      - run: aws s3 sync dist s3://micro-fe-fnnit/container/latest
        env:
          AWS_ACCESS_KEY_ID: AKIAXOEU6M7HSOKZB7WB
          AWS_SECRET_ACCESS_KEY: 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
          AWS_DEFAULT_REGION: ap-southeast-1
          AWS_EC2_METADATA_DISABLED: true
